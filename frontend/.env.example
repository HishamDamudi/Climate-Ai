# Copy this file to .env.local for local overrides, or set the same key in
# your Vercel project's Environment Variables for production.

# Leave unset locally to use the Vite dev proxy (see vite.config.js).
# In Vercel, set this to your deployed Render backend URL:
# VITE_API_BASE_URL=https://climate-ai-backend.onrender.com
VITE_API_BASE_URL=
